version: 2.1

workflows:
  deploy-to-ec2:
    jobs:
      - deploy:
          filters:
            branches:
              only:
                - develop
jobs:
  runner:
    machine: true
    resource_class: aqtech/cicd-class
    steps:
      - run: echo "Hi I'm on Runners!"
      